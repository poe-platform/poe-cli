{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Poe Agent Configuration",
  "description": "Configuration file for enabling or disabling Poe CLI sub-agents.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional schema reference for editor tooling."
    },
    "agents": {
      "type": "array",
      "description": "List of agent definitions and their enablement state.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for the agent."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether the agent is currently enabled."
          }
        },
        "required": ["id", "enabled"],
        "additionalProperties": false
      },
      "default": []
    }
  },
  "required": ["agents"],
  "additionalProperties": false
}
